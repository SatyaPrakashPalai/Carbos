import React from "react";
import styles from "./strip.module.css";

function Strip() {
  return (
    <div className={styles["strip"]}>
      <span>launching soon on web...</span>
    </div>
  );
}

export default Strip;
